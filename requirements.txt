# Зависимости времени выполнения ИС «ГрузПоток».
#
# Этот файл — единственный источник версий. Раздел [project] в pyproject.toml
# читает его через механизм dynamic-зависимостей setuptools, Dockerfile
# устанавливает пакеты прямо отсюда. Раньше список был продублирован
# в pyproject.toml и в Dockerfile, и расходились они молча.
#
# Установка в чистое окружение:
#     python -m venv .venv
#     .venv/bin/pip install -r requirements.txt

django==6.0.7
djangorestframework==3.17.1
drf-spectacular==0.30.0
psycopg[binary]==3.3.4
whitenoise==6.11.0
gunicorn==23.0.0
openpyxl==3.1.5
python-docx==1.2.0
reportlab==4.4.4
