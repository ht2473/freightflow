-- Расширения PostgreSQL для контура разработки.
--
-- Выполняется один раз при создании тома базы. Схему таблиц здесь не
-- создаём намеренно: её накатывают миграции Django. Контур разработки
-- должен воспроизводить тот путь, которым база создаётся на самом деле,
-- иначе расхождение между миграциями и сценарием обнаружится только
-- при развёртывании.

CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Ускоряет поиск по наименованиям объектов без учёта регистра и опечаток.
CREATE EXTENSION IF NOT EXISTS pg_trgm;
